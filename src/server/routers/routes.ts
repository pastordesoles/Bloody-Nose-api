const routes = {
  usersEndpoint: "/users",
  sessionsEndpoint: "/sessions",
  register: "/register",
  login: "/login",
  list: "/list",
  session: "/session/:id",
  createSession: "/add",
  deleteSession: "/delete/:id",
  editSession: "/edit/:id",
};

export default routes;
