const routes = {
  usersEndpoint: "/users",
  sessionsEndpoint: "/sessions",
  register: "/register",
  login: "/login",
  list: "/list",
  session: "/session/:id",
  createSession: "/add",
};

export default routes;
